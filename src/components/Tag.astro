---
import paths from "@/utils/paths";

interface Props {
  name: string;
}

const { name } = Astro.props;
---

<a class="tag" href={`/${paths.tag.url}/${name}/`}>
  {name}
</a>
<style>
  .tag {
    color: rgb(var(--gray));
    text-decoration: dotted underline;
  }
</style>
